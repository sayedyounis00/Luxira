import 'package:flutter/material.dart';

class HomeScreenPart2 extends StatelessWidget {
  const HomeScreenPart2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('HomeSereen part 2'),),
    );
  }
}